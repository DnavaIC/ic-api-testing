meta {
  name: Update Location
  type: http
  seq: 3
}

put {
  url: {{base-url}}/ats/contracts/{{contract-id}}/locations/{{location_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{congito-token}}
}

body:json {
  {
      "city": "Updated from postman",
      "state": "Updated from postman",
      "street_address": "test bubo 8352"
  }
}

settings {
  encodeUrl: true
}
