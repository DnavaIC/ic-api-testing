meta {
  name: Get Location
  type: http
  seq: 2
}

get {
  url: {{base-url}}/ats/contracts/{{contract-id}}/locations/{{location_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{congito-token}}
}

settings {
  encodeUrl: true
}
