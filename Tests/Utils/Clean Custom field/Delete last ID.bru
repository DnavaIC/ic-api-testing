meta {
  name: Delete last ID
  type: http
  seq: 2
}

delete {
  url: {{base-url}}/ats/contracts/{{contract-id}}/custom_field/{{custom_field_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{congito-token}}
}

settings {
  encodeUrl: true
}
